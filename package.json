{
  "name": "epochs",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A frontend for the Epochs contract on Ethereum mainnet — a rebuild of epochs.cosmiccomputation.org",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "wrangler deploy",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.49.0",
    "@cloudflare/workers-types": "^5.20260731.1",
    "@types/bun": "^1.3.0",
    "typescript": "^7.0.2",
    "vite": "^8.2.0",
    "wrangler": "^4.116.0"
  }
}
